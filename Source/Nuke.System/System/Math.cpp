
#include "_pch.h"
#include "Math.h"

namespace Nuke::System
{
    double Math::Round(double a)
    {
        return std::round(a);
    }

    float Math::Round(float a)
    {
        return std::round(a);
    }
}